#!/usr/bin/env bash

rdo_header common.help

# rdo__help__command
rdo__help__command() {
  local namespace="${FUNCNAME[1]}"
  local cmd_names=(${namespace//_/ })
  local current_cmd="${cmd_names[-1]}"

  echo
  echo "Usage:  ${namespace//_/ } COMMAND"


  # echo
  # echo "Commands"
  # echo "  args      List all args in build targets"
  # echo
  # echo "Run '${namespace//_/ } COMMAND --help' for more information on a command."
}
